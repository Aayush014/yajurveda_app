import 'package:flutter/material.dart';

class FavToday extends StatelessWidget {
  const FavToday({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Image.asset("Assets/Img/sadhu.png"),
      ),
    );
  }
}
